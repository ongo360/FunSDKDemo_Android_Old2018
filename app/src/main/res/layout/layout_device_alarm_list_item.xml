<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp" >

    <ImageView
        android:id="@+id/imgAlarmPicture"
        android:layout_width="80dp"
        android:layout_height="60dp"
        android:layout_marginLeft="5dp"
        android:layout_centerVertical="true"
        android:padding="2dp"
        android:scaleType="fitCenter"
        android:src="@drawable/icon_device"
        android:contentDescription="@string/device_type"
        />
    
    <TextView 
        android:id="@+id/txtAlarmTime"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:layout_centerVertical="true"
        android:padding="2dp"
        android:textSize="16dp"
        android:textColor="@color/demo_desc"
        />

    <LinearLayout
        android:layout_toRightOf="@id/imgAlarmPicture"
        android:layout_toLeftOf="@id/txtAlarmTime"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_margin="5dp"
        android:orientation="vertical" 
        >

        <TextView
            android:id="@+id/txtAlarmEvent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:textColor="@drawable/common_title_color"
            android:textSize="20dp" />
        
        <TextView
            android:id="@+id/txtAlarmStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:singleLine="true"
            android:textColor="@drawable/common_desc_color"
            android:textSize="14dp" />

        
    </LinearLayout>

</RelativeLayout>